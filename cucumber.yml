#encoding: UTF-8
---
    default: -p progress -p json -p dev
    
    
    progress: --format progress
    
    pretty: --format pretty
    html:  --format html --out=reports/report.html
    json:  --format json --out=reports/report.json
    
    local: ENVIRONMENT=local
    dev: ENVIRONMENT=dev
    hmg: ENVIRONMENT=hmg
    prod: ENVIRONMENT=prod